name: Deploy Bot

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    if: ${{ secrets.EC2_SSH_KEY != '' }}
    runs-on: ubuntu-latest

    env:
      DEPLOY_HOST: ec2-3-21-92-184.us-east-2.compute.amazonaws.com
      DEPLOY_USER: ubuntu
      DEPLOY_PATH: /opt/hotel-bot

    steps:

      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Validate JavaScript
        run: npm run check

      - name: Setup SSH
        run: |

          mkdir -p ~/.ssh

          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_ed25519

          chmod 600 ~/.ssh/id_ed25519

          ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts

      - name: Deploy files
        run: |

          rsync -avz --delete \
          --exclude=node_modules \
          --exclude=auth \
          --exclude=data/reservas.json \
          --exclude=data/closedDates.json \
          --exclude=data/calendarReservations.json \
          --exclude=data/groupMessages.json \
          --exclude=data/rackStatus.json \
          --exclude=data/*.db \
          --exclude=data/*.db-* \
          --exclude=logs \
          ./ "$DEPLOY_USER@$DEPLOY_HOST:$DEPLOY_PATH/"

      - name: Install and restart services
        run: |

          ssh "$DEPLOY_USER@$DEPLOY_HOST" "
          set -e &&
          cd '$DEPLOY_PATH' &&
          sudo apt-get update &&
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y tesseract-ocr imagemagick sqlite3 &&
          npm install --omit=dev &&
          sudo install -m 644 deploy/hotel-bot.service /etc/systemd/system/hotel-bot.service &&
          sudo install -m 644 deploy/hotel-dashboard.service /etc/systemd/system/hotel-dashboard.service &&
          sudo systemctl daemon-reload &&
          sudo systemctl enable hotel-bot hotel-dashboard &&
          sudo systemctl restart hotel-bot hotel-dashboard &&
          sudo systemctl --no-pager --full status hotel-bot hotel-dashboard
          "
